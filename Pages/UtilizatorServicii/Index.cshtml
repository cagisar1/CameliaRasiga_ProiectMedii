@page
@model CameliaRasiga_ProiectMedii.Pages.UtilizatorServicii.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UtilizatorServiciu[0].Utilizator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UtilizatorServiciu[0].Serviciu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UtilizatorServiciu[0].ReturnDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UtilizatorServiciu) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Utilizator.NumeComplet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Serviciu.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
